<div class="row">
  <div class="col-md-2">
      <%= link_to gravatar_for(current_user, size: Settings.size), current_user %>
  </div>
  <div class="col-md-10">
    <h3>
      <%= current_user.name %>
    </h3>
    <%= link_to t(".view"), current_user %><br>
    <%= pluralize current_user.microposts.count, t(".micropost") %>
  </div>
</div>
