<div id="like_form_<%= post.id %>">
  <%= form_for current_user.active_likes.build, remote: true do |f| %>
    <div><%= hidden_field_tag :micropost_id, post.id %></div>
    <%= f.submit t(".like"), class: "btn btn-secondary btn-sm rounded-0", id: "like" %>
  <% end %>
</div>
