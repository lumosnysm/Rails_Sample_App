<% if @feed_items.any? %>
  <%= paginate @feed_items, theme: "twitter-bootstrap-4" %>
  <ol class="microposts list-group list-group-flush">
    <%= render @feed_items %>
  </ol>
  <%= paginate @feed_items, theme: "twitter-bootstrap-4" %>
<% end %>
